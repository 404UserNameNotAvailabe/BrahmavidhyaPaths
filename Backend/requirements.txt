fastapi==0.116.1
uvicorn==0.35.0
jinja2==3.1.6
python-multipart==0.0.20
# pydantic 2.13+ ships wheels for Python 3.14; 2.11 does not.
pydantic>=2.13,<3
# psycopg3 (binary + pool) — has Python 3.14 wheels; psycopg2-binary does not.
psycopg[binary,pool]>=3.2
python-dotenv==1.0.1
google-genai>=1.21
